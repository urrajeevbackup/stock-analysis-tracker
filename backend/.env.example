APP_NAME=Stock Analysis Tracker API
API_V1_PREFIX=/api/v1

DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=3306
DB_NAME=stock_tracker

# Optional override (if provided, this takes precedence over DB_* values)
# DATABASE_URL=mysql+pymysql://root:root@localhost:3306/stock_tracker
