import { Link } from 'react-router-dom';import type { Trade } from '../../types/trade';import { fDate, fMoney } from '../../utils/formatters';
export default function TradeTable({trades}:{trades:Trade[]}){return <div className='overflow-auto bg-white rounded-xl shadow'><table className='min-w-full text-sm'><thead><tr className='text-left border-b'>{['Stock','Type','Entry','Exit','SL','Target','Status','Date',''].map(h=><th key={h} className='p-2'>{h}</th>)}</tr></thead><tbody>{trades.map(t=><tr key={t.id} className='border-b'><td className='p-2'>{t.stock_symbol}</td><td>{t.trade_type}</td><td>{fMoney(t.entry_price)}</td><td>{fMoney(t.exit_price)}</td><td>{fMoney(t.stop_loss)}</td><td>{fMoney(t.target_price)}</td><td>{t.status}</td><td>{fDate(t.entry_date)}</td><td><Link to={`/trades/${t.id}`} className='text-blue-600'>View</Link></td></tr>)}</tbody></table></div>}
