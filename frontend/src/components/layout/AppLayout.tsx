import { Link } from 'react-router-dom';
const nav = [['/','Dashboard'],['/analysis','Analysis'],['/log-trade','Log Trade'],['/trade-detail','Trade Detail'],['/live-monitor','Live Monitor'],['/alert-centre','Alert Centre'],['/reports','Reports']] as const;
export default function AppLayout({children}:{children:React.ReactNode}){return <div className="min-h-screen bg-slate-50 pb-16 md:pb-0"><div className="hidden md:flex"><aside className="w-64 min-h-screen bg-white border-r p-4 space-y-2">{nav.map(([to,l])=><Link key={to} to={to} className="block rounded-lg px-3 py-2 hover:bg-slate-100">{l}</Link>)}</aside><div className="flex-1"><header className="bg-white border-b p-4 font-semibold">Stock Analysis Tracker</header><main className="p-6">{children}</main></div></div><main className="md:hidden p-4">{children}</main><nav className="fixed md:hidden bottom-0 inset-x-0 bg-white border-t grid grid-cols-4 text-xs">{nav.slice(0,4).map(([to,l])=><Link key={to} to={to} className="p-3 text-center">{l}</Link>)}</nav></div>}
