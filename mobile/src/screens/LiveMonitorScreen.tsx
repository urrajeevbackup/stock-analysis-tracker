import { Text, View } from 'react-native';

export default function LiveMonitorScreen() {
  return (
    <View style={{ padding: 16 }}>
      <Text style={{ fontSize: 20, fontWeight: '600' }}>LiveMonitor</Text>
      <Text>Placeholder screen for LiveMonitor.</Text>
    </View>
  );
}
